# Backend API origin (REST contract + better-auth under /api/auth). Optional in
# dev — the client falls back to http://localhost:3000, which matches the local
# backend. Set it for deployed environments where the API lives elsewhere.
# VITE_API_URL=http://localhost:3000
